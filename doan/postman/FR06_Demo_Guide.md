# 🚀 Hướng Dẫn Kịch Bản Demo FR-06: System Administration (Manual Test)

Module Dashboard & Metrics này đặc biệt "ăn tiền" ở chỗ **bạn đã tìm ra được 4 con BUG rất thực tế** bằng kỹ thuật Test Thủ công (Manual). Hãy tập trung "khoe" 4 lỗi này với Hội đồng:

| Test Case ID | Các bước Demo chi tiết (Thao tác cụ thể & Lệnh Postman) |
| :--- | :--- |
| **FR06_UI_01<br>đến<br>FR06_UI_05** | **Kiểm tra Giao diện Dashboard (Trình duyệt):**<br>1. Đăng nhập tài khoản Admin, mở trang Dashboard (UI_01).<br>2. Bấm `F12` -> Bật chế độ Responsive (Điện thoại/Tablet) để show các thẻ Thống kê tự xếp dọc xuống rất mượt (UI_02).<br>3. Chỉ ra các con số lớn được format thông minh (VD: `1,000,000` thành `1M`) (UI_03).<br>4. Click qua lại thanh Sidebar trái, trang chuyển động mượt không bị load lại cả cục (React Router) (UI_05). |
| **FR06_FN_01<br>đến<br>FR06_FN_04** | **Kiểm tra Phân quyền Admin (Postman Manual):**<br>1. **Lệnh:** `GET http://localhost:8080/admin/stats`<br>2. Tab Auth dán Token của Admin -> Send -> Trả về `200 OK` (FN_01).<br>3. Đổi sang dán Token của Student -> Send -> Báo `403 Forbidden` (FN_02).<br>4. Chuyển qua Tab Headers chèn `X-User-Role = ADMIN` nhưng dùng Token sinh viên -> Báo `401/403` (Chống giả mạo) (FN_04). |
| **FR06_FN_05<br>đến<br>FR06_FN_11** | **Đồng bộ Dữ liệu Thời gian thực (Postman + Web):**<br>1. Chỉ cho Cô xem con số Tổng Biểu đồ (Total Diagrams) trên màn hình Admin hiện tại.<br>2. Mở Postman dùng Token Sinh viên gọi lệnh:<br>`POST http://localhost:8080/api/diagrams`<br>(Hoặc thao tác bấm tạo luôn 1 biểu đồ mới trên giao diện sinh viên).<br>3. Quay lại trang Admin F5 -> Tổng số Biểu đồ lập tức cộng thêm 1 (FN_09).<br>4. Thao tác xóa vĩnh viễn biểu đồ đó đi -> Trang Admin giảm về số cũ ngay lập tức (FN_11). |
| **FR06_FN_14** | 🚨 **[BUG 1] Lỗi quên khai báo API (Postman Manual):**<br>1. **Lệnh:** `GET http://localhost:5000/metrics` (Trỏ thẳng vào Chat-service).<br>2. Tab Auth để trống. Bấm Send -> Báo lỗi `404 Not Found` đỏ chót.<br>3. **Báo cáo Cô:** *"Khi em test tích hợp bằng Postman, em phát hiện backend Python chưa viết route xử lý API này, nên nó văng ra một trang HTML báo lỗi của Flask thay vì JSON. Em đã log lỗi này vào báo cáo"*. |
| **FR06_FN_28<br>đến<br>FR06_FN_32** | 🚨 **[BUG 2] Lỗi nuốt ngoại lệ Validation (Postman Manual):**<br>1. **Lệnh:** `GET http://localhost:5000/account/search?limit=abc`<br>2. Cố tình gõ trực tiếp chữ `abc` vào biến `limit` trên thanh URL.<br>3. Bấm Send -> Bất ngờ trả về `200 OK` thay vì báo lỗi cú pháp `400 Bad Request`.<br>4. **Báo cáo Cô:** *"Em test tay truyền tham số dạng chữ vào biến Limit, backend không báo lỗi 400 mà lẳng lặng gán ngầm limit=10. Đây là lỗi bưng bít ngoại lệ rất nguy hiểm"*. |
| **FR06_FN_44** | 🚨 **[BUG 3] Rò rỉ dữ liệu Người dùng (MySQL Manual):**<br>1. Mở phần mềm MySQL Workbench. Mở giao diện cấu trúc của bảng `chat_metrics`.<br>2. **Báo cáo Cô:** *"Thưa cô, trong bảng lưu vết Chat này KHÔNG HỀ CÓ cột `user_id`. Điều này dẫn tới thảm họa là Input/Output chat của mọi sinh viên bị nhốt chung 1 mẻ, vi phạm tính riêng tư của hệ thống (Tenant Scope Leaks)"*. |
| **FR06_FN_45** | 🚨 **[BUG 4] Sập máy chủ do tải nặng (Báo cáo miệng / F5 nhanh):**<br>1. **Thao tác:** Bấm giữ nút F5 liên tục trên trang Web Admin, hoặc gõ tay ấn nút Send trên Postman thật nhanh liên tục 20 lần.<br>2. **Báo cáo Cô:** *"Khi em test tay bằng cách gửi truy vấn liên tục vào API `/admin/stats`, hệ thống sẽ văng lỗi 500 Internal Error. Lý do là code Spring Boot mở truy vấn Database mà không cấu hình Connection Pool hợp lý, làm cạn kiệt tài nguyên máy chủ"*. |
| **FR06_FN_15<br>đến<br>FR06_FN_27<br>& Các case còn lại**| **Tính toán Metrics & Cắt chuỗi (Thao tác trên Web):**<br>1. Mở giao diện Chat AI, nhập 1 đoạn văn bản siêu dài (hơn 150 ký tự).<br>2. Mở MySQL check bảng logs, chỉ cho cô thấy chuỗi đã tự động bị cắt ngắn đúng 100 ký tự (FN_26).<br>3. Báo cáo nhanh: *"Các phần tính công thức Toán như Tỉ lệ lỗi (Error Rate) hay chia cho 0, em đã tự tay nhập các số liệu giả vào Database bằng Workbench và tính lại bằng máy tính cầm tay, kết quả hiển thị trên màn hình Web khớp 100% ạ."* |
