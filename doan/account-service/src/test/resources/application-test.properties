spring.application.name=account-service-test
server.port=0

# H2 Database
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Disable external services
eureka.client.enabled=false
spring.kafka.bootstrap-servers=
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# JWT
jwt.secret=testSecretKeyForJwtTokenThatShouldBeAtLeast256BitsLongToEnsureSecurityAndProperFunctioning
jwt.expiration=86400000

# Console output với ANSI colors
spring.output.ansi.enabled=ALWAYS

# Sửa lại api-gateway-url (thêm đường dẫn nếu muốn chạy test trên Codespaces)
# accountservice.api-gateway-url=
# accountservice.account-service-url=${ACCOUNT_SERVICE_URL:http://localhost:8082}